# VizinhoAlert Environment Variables
# Copy this file to .env and update the values

# API Base URL - MUST use HTTPS in production
# This is the ONLY required environment variable
EXPO_PUBLIC_API_BASE_URL=https://api.vizinhoalert.eu/api/v1

# For local development (mobile only - web requires HTTPS)
# EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# NOTE: Web browsers require HTTPS for API calls (mixed content policy)
# Mobile apps can use HTTP for local development but HTTPS is recommended
